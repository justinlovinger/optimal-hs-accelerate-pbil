name:                optimal-pbil
version:             0.1.0.0
github:              "JustinLovinger/optimal-pbil"
license:             MIT
author:              "Justin Lovinger"
maintainer:          "git@justinlovinger.com"
copyright:           "Justin Lovinger"

extra-source-files:
- README.md
- ChangeLog.md

# Metadata used when publishing your package
# synopsis:            Short description of your package
# category:            optimization

# To avoid duplicated efforts in documentation and dealing with the
# complications of embedding Haddock markup inside cabal files, it is
# common to point users to the README.md file.
description:         Please see the README on GitHub at <https://github.com/JustinLovinger/optimal-pbil#readme>

dependencies:
  - base >= 4.7 && < 5

library:
  source-dirs: src

tests:
  optimal-pbil-test:
    main:                Spec.hs
    source-dirs:         test
    ghc-options:
      - -threaded
      - -rtsopts
      - -with-rtsopts=-N
    dependencies:
      - optimal-pbil
